'use strict';

const MP_EVENTS = {
	core: {
		launch: `App Launch`,
		exit: `App Exit`
	},
	ui: {
		modalBackgroundContentViewed: `Modal Background Content Viewed`
	}
};

module.exports = { MP_EVENTS };
